import { Route } from '@angular/router';
import { CodeValidationComponent } from './code-validation.component';

export const codeValidationRoutes: Route[] = [
    {
        path: '',
        component: CodeValidationComponent,
    },
];
